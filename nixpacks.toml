[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "python3Packages.virtualenv", "bash"]

[phases.install]
cmds = [
  "chmod +x build.sh",
  "bash build.sh"
]

[start]
cmd = "python3 -m uvicorn avenai_final:app --host 0.0.0.0 --port $PORT"
